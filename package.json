{
  "name": "brainly-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter frontend dev\" \"pnpm --filter backend dev\"",
    "build": "pnpm --filter db db:generate && pnpm --filter db build && pnpm --filter backend build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
